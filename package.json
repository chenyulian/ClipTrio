{
  "name": "clip-trio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check server-core.js && node --check server-process.js && node --check public/app-core.js && node --check scripts/media-tools.js && node --check scripts/smoke-core.js && node --check scripts/generate-fixtures.js && node --check scripts/smoke-render.js",
    "fixtures": "node scripts/generate-fixtures.js",
    "smoke:render": "node scripts/smoke-render.js",
    "smoke:render:direct": "node scripts/smoke-render.js --target=direct",
    "smoke:render:proxy": "node scripts/smoke-render.js --target=proxy"
  },
  "engines": {
    "node": ">=18"
  }
}
