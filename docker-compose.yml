services:
  live-triptych:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - FFMPEG_PATH=ffmpeg
      - FFPROBE_PATH=ffprobe
    restart: unless-stopped
